<div class="processes-create__additional-inputs">
    @if ($statusStage > 1)
        <h1>ПО inputs</h1>
    @endif

    @if ($statusStage > 2)
        <h1>АЦ inputs</h1>
    @endif

    @if ($statusStage > 3)
        <h1>СЦ inputs</h1>
    @endif

    @if ($statusStage > 4)
        <h1>Кк inputs</h1>
    @endif
</div>
