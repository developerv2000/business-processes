<div class="processes-create__additional-inputs">
    @if ($processStage > 1)
        <h1>ПО inputs</h1>
    @endif

    @if ($processStage > 2)
        <h1>АЦ inputs</h1>
    @endif

    @if ($processStage > 3)
        <h1>СЦ inputs</h1>
    @endif

    @if ($processStage > 4)
        <h1>Кк inputs</h1>
    @endif
</div>
